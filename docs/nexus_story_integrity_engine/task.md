# タスクリスト：フェーズ 2 - 物語整合性エンジン

## フェーズ 1.5: 言語ハブの最終調整 (完了)
- [x] Tomarigi 辞書の全量マージ (166ルール)
- [x] LanguageTool 辞書の全量マージ (491ルール)
- [x] 高度な文法検知エンジン (JS) の実装
- [x] textlint 実行環境の安定化 (シンボリックリンク方式)

## フェーズ 2: 物語整合性エンジン (未着手)

### 1. プロジェクト識別と隔離
- [ ] `ProjectManager` クラスの作成 (パスによる作品境界判定)
- [ ] ChromaDB のマルチコレクション対応 (作品ごとの隔離)
- [x] パスに基づくプロジェクト自動識別 (`ProjectManager`)
- [x] プロジェクト単位の知識ベース隔離 (ChromaDB Filter)

### 2. 知識抽出 (Fact Extraction)
- [x] 設定資料からの「事実（属性）」抽出ロジックの強化
- [ ] タグ (#設定, #プロット) に基づく重み付け登録ロジック
- [ ] 100行制限を撤廃したバックグラウンド全件スキャン

### 3. 監査ロジックの実装
- [x] 矛盾検知時のアラート生成 (Layer 2 監査)

### 4. UI 統合
- [x] `AuditReportWindow` への Layer 1 (textlint) 結果の統合
- [x] `AuditReportWindow` への Layer 2 (物語監査) 結果の統合
- [x] AI による確認バナー (`KnowledgeSuggestionBanner`) の作成
- [ ] 知識の関連付け取り消し (Undo) 機能

### 5. 検証
- [ ] 複数プロジェクト混在時の隔離テスト
- [ ] 設定矛盾の検知精度テスト
