# タスクリスト：NEXUS 無敵の校正・監査エンジン (フェーズ 1)

## フェーズ 1.1：高速ハイブリッドエンジンの基盤構築
- [x] **[事前検証]** 既存挙動のスナップショット取得 (`audit_proofreader_behavior.py`)
- [x] **[基盤実装]** `Proofreader` クラスに `fast_rules` と `_run_fast_scan` を追加
- [x] **[統合]** `proofread()` への統合と、既存ルール（正規表現）との共存
- [x] **[検証]** 回帰テストをパスし、既存機能を壊していないことを確認
- [x] **[ロールバック策]** シンプルな構造を維持し、いつでも元のリスト形式に戻せる状態を確保

## フェーズ 1.2：ルールの段階的移行と拡張
- [ ] **[移行]** `novel_specific_rules` の残り（リテラル可能なもの）を `fast_rules` へ
- [ ] **[拡張]** 外部ツール（textlint等）から主要な 100 件のルールを抽出・追加
- [ ] **[検証]** 大規模ルール（1000件以上）でのパフォーマンス測定

## フェーズ 1.3：外部ルールインポートの自動化
- [ ] **[ツール作成]** LanguageTool XML から NEXUS JSON への変換スクリプト作成
- [ ] **[検証]** インポートしたルールでの誤検知率チェック
